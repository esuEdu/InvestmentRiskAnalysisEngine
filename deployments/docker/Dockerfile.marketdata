FROM golang:1.26-alpine AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN go build -o /marketdata ./cmd/marketdata

FROM alpine:latest
RUN apk --no-cache add ca-certificates tzdata
COPY --from=builder /marketdata /marketdata
EXPOSE 8081
ENTRYPOINT ["/marketdata"]
